// Export user routes
export { userModuleRoutes as userRoutes } from './routes'

// Export user types and DTOs
export * from './application/dto'

// Export user controller for testing
export { createUserController } from './container'
